name: gym_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

fl_dependencies:
  flutter:
    sdk: flutter
    fl_chart: ^0.68.0

Google_dependencies:
   flutter:
    sdk: flutter
    google_maps_flutter: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.68.0
  google_maps_flutter: ^2.5.0
  geolocator: ^14.0.0
  http: ^1.2.2